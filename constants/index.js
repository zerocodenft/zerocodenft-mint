const SALE_STATUS = Object.freeze({
	Paused: 0,
	Presale: 1,
	Public: 2,
})

const MINT_SELECTOR_TYPE = Object.freeze({
	Range: 0,
	SpinButton: 1
})

export {
    SALE_STATUS,
    MINT_SELECTOR_TYPE
}