const SALE_STATUS = Object.freeze({
	Paused: 0,
	Presale: 1,
	Public: 2,
})

const MINT_SELECTOR_TYPE = Object.freeze({
	SpinButton: 0,
	Range: 1
})

export {
    SALE_STATUS,
    MINT_SELECTOR_TYPE
}